<nav class="pagination clearfix" role="navigation">
  {{#if prev}}
    <a class="pagination-newer" href="{{page_url prev}}">&larr; Newer Posts</a>
  {{/if}}
  {{#if next}}
    <a class="pagination-older" href="{{page_url next}}">Older Posts &rarr;</a>
  {{/if}}
</nav>
